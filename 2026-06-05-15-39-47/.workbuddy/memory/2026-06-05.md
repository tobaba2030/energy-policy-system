# 2026-06-05

## 安装 Skills 和 CLI 工具

### Skills（安装到 ~/.agents/skills/）
- **handoff** ✅ 来自 mattpocock/skills
- **claude-skill** ✅ 来自 feiskyer/codex-settings
- **deep-research** ✅ 来自 feiskyer/codex-settings
- **youtube-transcribe-skill** ✅ 来自 feiskyer/codex-settings

注意：feiskyer skills 安装时会尝试写入 Trae CN 目录（被沙盒拒绝），但核心文件已写入 `~/.agents/skills/`，功能正常。

### CLI 工具
- **twitter-cli v0.8.5** ✅ 通过 `uv tool install`（使用清华镜像）
  - 命令：`twitter`（需将 `~/.local/bin` 加入 PATH）
- **xiaohongshu-cli v0.6.4** ✅ 通过 `uv tool install`（使用清华镜像）
  - 命令：`xhs`（需将 `~/.local/bin` 加入 PATH）
- **xfetch-cli** ❌ 需用户手动安装（需 node-gyp 构建，沙盒限制）
  - 手动安装命令：`npm install -g xfetch-cli`

### uv 安装路径
- `C:\Users\jianlinw\.local\bin`（需加入 PATH）

### 安装技巧
- uv tool install 遇到网络问题时，使用 `--index-url https://pypi.tuna.tsinghua.edu.cn/simple`
- npx skills add feiskyer 系列需要 GitHub 连接畅通，不稳定时多重试几次
